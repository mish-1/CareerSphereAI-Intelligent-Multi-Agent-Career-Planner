"""Skill analysis agent placeholder."""


def analyze_skills(user_profile: dict) -> dict:
    """Return a basic skill analysis payload."""
    return {
        "summary": "Skill analysis not yet implemented.",
        "input_keys": sorted(user_profile.keys()),
    }
